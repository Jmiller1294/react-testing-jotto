import moxios from 'moxios';

describe('getScretWord', () => {
  beforeEach(() => {
    moxios.install();
  })
  afterEach(() => {
    moxios.uninstall();
  })

  test('secretWord is returned from the function', () => {
    
  })
})